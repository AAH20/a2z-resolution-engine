# Security

Report vulnerabilities through GitHub Security Advisories. The built-in API binds to loopback and needs a customer-controlled TLS/identity proxy before remote use. Keep gateway keys, customer knowledge, tickets, evaluation labels and the SQLite event database out of this public repository. The application does not authenticate knowledge authors or reviewers, verify consent, or redact personal data from input; obtain those controls before a real pilot.
